import StackViewLayout from "../Layout/Private/StackView/StackViewLayout";

const GetAnsFromUser = ({ question }) => {
  return <StackViewLayout question={question} />;
};

export default GetAnsFromUser;
